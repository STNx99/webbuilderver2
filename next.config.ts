import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  allowedDevOrigins: ["https://webbuilderapigo-production.up.railway.app/api/v1", "https://basilisk-needed-usually.ngrok-free.app"]
};

export default nextConfig;
